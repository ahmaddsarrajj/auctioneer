import { SuccessCodeInterceptor } from './success-code.interceptor';

describe('SuccessCodeInterceptor', () => {
  it('should be defined', () => {
    expect(new SuccessCodeInterceptor()).toBeDefined();
  });
});
