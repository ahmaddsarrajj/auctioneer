import { CouponInterceptorInterceptor } from './coupon-interceptor.interceptor';

describe('CouponInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new CouponInterceptorInterceptor()).toBeDefined();
  });
});
